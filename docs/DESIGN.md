# OPA-driven FHIR log masking — Design

See the source repo for the full design document with mermaid diagrams.
This stub points to the canonical version in this commit; refer to the
workspace's `docs/DESIGN.md` (committed alongside) for details on:

- Component overview (mermaid flowchart)
- Data flow for one request (mermaid sequence diagram)
- HTTP pipeline detail for Service A (mermaid flowchart)
- Threat model and OPA-as-CNCF discussion

(The full DESIGN.md is committed via this commit; this stub will be
replaced by a follow-up.)
